{
    "scripts": {
        "build": "yarn && npm install prisma --save && npx prisma migrate dev --name itermob && npx prisma migrate deploy && npx prisma db pull && npx prisma generate && npm install @prisma/client --save",
        "start": "node app.js",
        "dev": "nodemon app.js"
    },
    "dependencies": {
        "@prisma/client": "^5.19.1",
        "body-parser": "^1.20.2",
        "cors": "^2.8.5",
        "express": "^4.21.0",
        "prisma": "^5.19.1"
    },
    "devDependencies": {
        "nodemon": "^3.1.4"
    }
}
